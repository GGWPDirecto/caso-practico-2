# Fichero de inventario en formato YAML (ansible/hosts.yml)
---
all:
  vars:
    acr_login_server: "tu-servidor-acr.azurecr.io"
    acr_username: "tu-usuario-acr"
    acr_password: "tu-password-acr"
    nginx_image: "{{ acr_login_server }}/webapp:casopractico2"

vm:
  hosts:
    # Pega aquí la IP pública de tu VM
    52.142.123.45:
      ansible_user: azureuser
      ansible_ssh_private_key_file: private_key.pem